# TODO: zero test coverage for UI tabs (project, focus, country, states, etc.),
# the sliding tab widget, theme system, settings persistence, ImageMagick paths,
# and map_gen workers. Add smoke tests that instantiate each tab without crashing,
# plus unit tests for undo/redo commands (GenericCommand, SetPropertyCommand, etc.).
